# Host user/group IDs for www-data inside the PHP container.
# Run `id -u` and `id -g` on the host and set the values below.
PUID=1000
PGID=1000

# Nginx
NGINX_PORT=8090

# PostgreSQL
POSTGRES_DB=web-counter
POSTGRES_USER=web-counter
POSTGRES_PASSWORD=secret
POSTGRES_PORT=2504
